var pi = Math.PI;      //3.141592653589793
Math.round(4.4);       // = 4 (rounded)
Math.round(4.5);       // = 5
Math.pow(2, 8);        // = 256 - calculate the power of a number 'Math.pow(base, exponent)'
Math.sqrt(49);         // = 7 - calculate the square root of a number (racine carrée)
Math.abs(-5.6);        // = 5.6 - calculate the absolute value of a number
Math.ceil(5.12);       // = 6 - round up a number (to the nearest integer greater than or equal to the given number)
Math.floor(2.99);      // = 2 - round down a number (down to the nearest integer)
Math.sin(0);           // = 0 - sine of a number (renvoie le sinus d'un angle en radians)
Math.cos(1);           // = -1 -calculate the cosine of a number (renvoie le cosinus de l'angle spécifié en radians)
Math.min(0, 3, -2, 1); // = -2 - find the minimum value among a list of numbers
Math.max(0, 3, -2, 1); // = 3 - find the maximum value among a list of numbers
Math.log(1);           // = 0 - calculate the natural logarithm
Math.exp(1);           // = 2.71828  - calculate the value of the mathematical constant e raised to the power of a given number (fonction exponentielle)
Math.random();         // random number between 0 and 1
Math.floor(Math.random() * 5) + 1; //random integer from 1 to 5 